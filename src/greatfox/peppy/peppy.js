/* Dropdown */

/*
  Goal: dropdown should not go offscreen and should attach to trigger point
*/

var Peppy = function() {
  "use strict";
  var init = function(opt) {
    console.log("Peppy init");
  };

  return {
    init: init
  };
};

module.exports = Peppy;
